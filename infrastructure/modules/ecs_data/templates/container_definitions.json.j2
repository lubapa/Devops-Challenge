[
  {
    "name": "${container_name}",
    "image": "${container_image}",
    "memory": ${memory},
    "cpu": ${cpu},
    "portMappings": [
      {
        "containerPort": 8080,
        "hostPort": 8080
      }
    ]
  }
]
